[tool.pytest.ini_options]
generate_report_on_test = true
addopts = "-vs -m 'not pynput' --html=/builds/worker/artifacts/report.html"
markers = [
    "pynput: test uses pynput package"
]
testpaths = [
    "tests"
]

[tool.ruff]
target-version = "py310"

[tool.pytest.ini_options]
generate_report_on_test = true
markers = [
    "pynput: test uses pynput package"
]
testpaths = [
    "tests"
]
addopts = "-vs -m 'not pynput' --run-headless --html=/builds/worker/artifacts/report.html"

[tool.ruff]
target-version = "py310"

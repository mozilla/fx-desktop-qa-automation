name: Check new RC version

on:
  schedule:
    - cron: "55 */4 * * *"

  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry-Run - If true, only report the plan; do not trigger downstream workflows."
        required: false
        default: "false"
      preview_only:
        description: "Preview Only - If true, only show which jobs would run (no reusable workflows called)"
        required: false
        default: "false"

env:
  LATEST: ""
  DRY_RUN: ${{ github.event_name == 'workflow_dispatch' && inputs.dry_run || false }}

permissions:
  contents: "write"

jobs:
  Check-RC-Version:
    runs-on: ubuntu-latest
    outputs:
      win_reportable: ${{ steps.reportable.outputs.win }}
      mac_reportable: ${{ steps.reportable.outputs.mac }}
      preview_json: ${{ steps.reportable.outputs.preview_json }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check if the run is reportable
        id: reportable
        env:
          FX_CHANNEL: "rc"
          STARFOX_SPLIT: smoke
          TESTRAIL_REPORT: true
          TESTRAIL_BASE_URL: ${{ secrets.TESTRAIL_BASE_URL }}
          TESTRAIL_API_KEY: ${{ secrets.TESTRAIL_API_KEY }}
          TESTRAIL_USERNAME: ${{ secrets.TESTRAIL_USERNAME }}
          DRY_RUN: ${{ env.DRY_RUN }}
        run:
          bash ./scripts/check_reportable.sh

  Run-Win-Smoke:
    needs: Check-RC-Version
    if: |
      ${{
        needs.Check-RC-Version.outputs.win_reportable == 'True' &&
        !( github.event.inputs.preview_only == 'true' )
       }}
    uses: ./.github/workflows/main.yml
    with:
      channel: "rc"
      job_to_run: Test-Windows
      dry_run: ${{ github.event.inputs.dry_run == 'true' }}
      test_set: smoke
    secrets: inherit

  Run-Mac-Smoke:
    needs: Check-RC-Version
    if: |
      ${{
        needs.Check-RC-Version.outputs.mac_reportable == 'True' &&
        !( github.event.inputs.preview_only == 'true' )
       }}
    uses: ./.github/workflows/main.yml
    with:
      channel: "rc"
      job_to_run: Test-MacOS
      dry_run: ${{ github.event.inputs.dry_run == 'true' }}
      test_set: smoke
    secrets: inherit
